name: RoutePilot AI CI/CD Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test-and-build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Set up Python 3.11
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest httpx python-multipart

    - name: Run Production Verification Suite
      run: |
        PYTHONPATH=. python tests/test_route_analysis.py
        PYTHONPATH=. python tests/test_command_center_3d.py
        PYTHONPATH=. python tests/test_circular_supply_chain.py

    - name: Build Docker Image
      run: |
        docker build -t routepilot-ai:latest .

    - name: Validate Container Health
      run: |
        docker run -d --name test-app -p 8000:8000 routepilot-ai:latest
        sleep 5
        curl -f http://localhost:8000/api/v1/monitoring/health-status || exit 1
        docker stop test-app
